"""Account state dashboard — manual entry + live floor/headroom display.

Standalone from L1-L4: it only needs AccountState + the Risk Engine's floor
math, both of which existed from P0. No Context or Judgment Engine required.
"""
